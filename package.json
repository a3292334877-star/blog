{
  "name": "blog",
  "version": "1.0.0",
  "type": "module",
  "description": "Sakikoの博客 - VitePress",
  "scripts": {
    "check": "node scripts/check-project.mjs",
    "dev": "npm run check && node scripts/generate-covers.mjs && node .vitepress/generate-music.mjs && vitepress dev",
    "build": "npm run check && node scripts/generate-covers.mjs && node .vitepress/generate-music.mjs && vitepress build && node .vitepress/generate-rss.mjs",
    "preview": "vitepress preview"
  },
  "keywords": [],
  "author": "Sakiko",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "gray-matter": "^4.0.3",
    "live2d-widget": "^3.0.4",
    "markdown-it-mathjax3": "^4.3.2",
    "medium-zoom": "^1.1.0",
    "vitepress": "^1.6.4"
  },
  "devDependencies": {
    "sass-embedded": "^1.100.0"
  }
}
